/**
 * ASYNCRONUS FUNCTIONS:
 * functions that run in background with other thread being asigned
 * are called asyncronus functions.
 * these are not in sync with the other js code
 * ex:
 * setInterval() , setTimeOut() ...
 * 
 * SYNCRONUS FUNCTIONS:
 * functions for whose execution the JS code execution waits, is called SYNCRONUS
 * ex: forEach() , 
 * //* by default any function we create is syncronuous
 * 
 * Usefull to understand multi threading in js
 */

// f1 = () 