#include "../../DSA/util/recursion_utilities.cpp"
using namespace std;

int main(){
    // unordered_set<string> s;
    // s.insert("aaa");
    // s.insert("aaa");
    // s.insert("aaa");
    // s.insert("aaa");
    // w(s); // (aaa)1 

    // unordered_set<vector<int>> ss;
    // // ss.insert(s);
    // // ss.insert(s);
    // w(ss); // (aaa)1 
    cout<<(int)'b';
}